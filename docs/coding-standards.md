# Coding Standards

- Node.js LTS + TypeScript strict
- proxy must never block on logging/telemetry
- never log secrets; redact and truncate
- map errors to stable categories
- keep dependencies minimal and boring
