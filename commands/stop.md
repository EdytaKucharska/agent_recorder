---
description: Stop the agent recorder daemon
---

Stop the agent recorder daemon and finalize the current recording session.

Run this command to stop recording:

```bash
npx agent-recorder stop
```

This will gracefully shut down the recorder and mark the current session as completed.
