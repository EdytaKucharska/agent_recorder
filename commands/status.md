---
description: Check if the agent recorder daemon is running
---

Check the status of the agent recorder daemon.

Run this command to check status:

```bash
npx agent-recorder status
```

This will show whether the daemon is running, its PID, and the current recording session if active.
