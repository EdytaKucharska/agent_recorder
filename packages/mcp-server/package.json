{
  "name": "@agent-recorder/mcp-server",
  "version": "2.0.14",
  "description": "MCP server for Agent Recorder — exposes observability data and event ingestion",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-recorder/core": "workspace:*",
    "@agent-recorder/types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.28.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0",
    "better-sqlite3": "^11.7.0",
    "fastify": "^5.0.0"
  }
}
