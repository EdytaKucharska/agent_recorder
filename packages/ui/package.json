{
  "name": "@agent-recorder/ui",
  "version": "2.0.8",
  "description": "Web UI for Agent Recorder",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-recorder/core": "workspace:*"
  }
}
