{
  "name": "@agent-recorder/service",
  "version": "2.0.8",
  "description": "Local daemon for Agent Recorder (MCP proxy + REST API)",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@agent-recorder/core": "workspace:*",
    "better-sqlite3": "^11.7.0",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.12",
    "@types/node": "^22.0.0"
  }
}
