/**
 * Event types for Agent Recorder.
 * Re-exported from @agent-recorder/types (the canonical zero-dep source).
 */

export type {
  EventType,
  EventStatus,
  ErrorCategory,
  BaseEvent,
  AgentCallEvent,
  SubagentCallEvent,
  SkillCallEvent,
  ToolCallEvent,
  RecordedEvent,
} from "@agent-recorder/types";
