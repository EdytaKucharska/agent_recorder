[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node dist/index.js mcp-server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
