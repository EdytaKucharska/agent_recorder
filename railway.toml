[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node packages/cli/dist/index.js mcp-server"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
